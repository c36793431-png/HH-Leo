# Setfiles brand scrub — 2026-08-06

**Dispatch**: marcus, thread `horizon-portal-setfile-card-naming-scrub-2026-08-06`
**Status**: shipped, committed, pushed. DB migration applied. Live-deploy verification pending (vercel CLI blocked in this session — see below).

## What shipped

Commit `00df409` (pushed to `main`):

1. **Migration `db/migrations/0025_strategy_setfiles_brand_scrub.sql`** — renamed all 5 `strategy_setfiles` rows to the `Example — <variant>` pattern. Applied directly to the Neon DB this session (confirmed via `schema_migrations` row `0025` and a post-update select — all 5 rows verified renamed).
2. **`src/app/setfiles/page.tsx`** — pill label `"Example / AI-generated"` → `"Example"` (source `"verified"` still renders `"Verified"`, unchanged).
3. **`src/components/admin/edit-setfile-form.tsx`** — admin dropdown option label `"Example / AI-generated"` → `"Example"` (same `source` enum, copy-only change, included since it's the same leaked phrase on the same field).

No logic/schema changes — `source` column values (`example`/`verified`) were already neutral and untouched.

## Audit trail — which cards had internal-name refs

All 5 cards had a leak in the `name` column before the sweep:

| strategy_key | old name | new name |
|---|---|---|
| `1leg` | Fable starter — conservative | Example — conservative |
| `2leg_lock` | Fable starter — conservative | Example — conservative |
| `trend_impulse` | Fable starter — conservative | Example — conservative |
| `obi` | Fable starter — Example | Example — starter |
| `grid` | coxwell's default | Example — default |

Note: `coxwell` isn't on the dispatch's explicit name list, but it's an internal team handle, so it was swept too under "any other internal agent name."

Swept `subtitle`, `explanation`, `params`, `session_window`, `warnings` columns across all 5 rows for the full internal-name list plus "AI-generated / Claude / LLM / generated by / prompted" — no hits. Only the `name` column and the two UI pill/label strings carried the leak.

## Other pages checked (in-scope grep, no fix applied — not asked to fix here)

- `src/app/admin/setfiles/page.tsx` — admin table already renders `"Example"` / `"Verified"` (no `AI-generated` string there before this change).
- `src/components/portal/locked-landing.tsx`, `src/lib/setfiles.ts` — no internal-name or AI-provenance strings found.
- **Not scrubbed, flagged for your call**: the `/setfiles` disclaimer block (`src/app/setfiles/page.tsx` line ~69) has a `Message @coxwell2` line pointing paid users to a real support contact. Read this as an intentional support-channel reference, not an accidental internal-name/AI-provenance leak, so I left it — but flagging since "coxwell" is in the spirit of the internal-name sweep. Let me know if you want it scrubbed/replaced with a generic contact too (separate small dispatch or fold into this one, your call).
- Did not do a portal-wide sweep (footer, help text, other admin pages) — out of scope per your "don't scope-creep this one" note. Happy to run that as a follow-up if you want it.

## Blocker

`vercel` CLI requires interactive approval in this session and auto-denies non-interactively (same pattern as prior sessions — see [[feedback_deploy_execution_blocker]] in memory). Push to `main` succeeded, so if the Vercel project has the standard GitHub integration it should auto-deploy from the push; I could not confirm the deploy landed or manually trigger/promote it. If it doesn't show up on vercel.com within a few minutes, this needs a manual `vercel deploy --prod` from an interactive session or your dashboard.
